# Dependencies
node_modules/
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build outputs
lib/
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2-*.log

# Testing
coverage/
.nyc_output/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temp
temp/
tmp/
*.tmp

# Secrets
rawWallet.json
wallet.json
*.jwk
*.pem
*.key

# Database
*.db
*.sqlite

# Docker volumes
*-data/

# PM2
.pm2/

# Operational test wallets (private keys — never commit)
ops-test-wallet*.json

# Backup output (contains wallets, .env, and user data — never commit)
backups/

# Claude Code: keep shared skills + settings.json, ignore personal local settings
.claude/settings.local.json
# turbo-bundler deployment overlay skill — source of truth lives in
# permaweb-services/turbo-bundler/skill/, symlinked in on this box only
.claude/skills/turbo-bundler

# perf baseline run outputs
scripts/perf/results/
