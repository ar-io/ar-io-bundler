# Metrics-scraper allowlist — the OTel/Prometheus collector egress CIDRs (+ admin,
# tailnet, loopback). Included inside each metrics location. Keep in sync with the
# fleet firewall (private-network-ar-io-node :9100 source list).
allow 127.0.0.1;          # on-box self-checks
allow 100.64.0.0/10;      # Tailscale (admin scraping from local)
allow 54.166.111.219;     # collector egress
allow 44.197.244.119;     # collector egress
allow 34.205.91.20;       # collector egress
allow 34.192.58.42;       # collector egress
allow 18.232.24.99;       # collector egress
allow 52.70.33.161;       # collector egress
allow 2.85.244.75;        # admin/office
deny all;
